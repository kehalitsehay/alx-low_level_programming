#include "main.h"

/**
 * main - entry point
 * mul: collaboration is multiplication
 * @a : is a variable
 * @b : is an other variable
 * description: mul returns  products of two number
 * Return: the product of a and b
 */
int mul(int a, int b)
{
	int product;

	product = a * b;
	return (product);
}
